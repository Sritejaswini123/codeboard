// import factory from "../factory.js";
// import { login, profile, refreshTokenHandler } from "../handlers/authHandler.js";
// import { authMiddleware } from "../utils/authMiddleware.js";

// const loginRoutes = factory.createApp();

// loginRoutes.post("/login", ...login);
// loginRoutes.get("/signup", authMiddleware, ...profile);
// loginRoutes.post("/refresh-token", ...refreshTokenHandler);


// export default loginRoutes;
